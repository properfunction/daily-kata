// Your task is to make two functions (max and min) that receive a list of integers as input and return, respectively, 
// the largest and lowest number in that list. You may consider that there will not be any empty arrays/vectors.

const min = (list) => Math.min(...list);
const max = (list) => Math.max(...list);